// ARRAYS - coleção de todo tipo de objetos, strings, números, datatypes, etc

let nomes = ["joao", "ana", "carlos"]

console.log(nomes)      // todos elementos
console.log(nomes[1])   // posição 1 'ana'
console.table(nomes)    // gera uma tabela com os índices e seus valores

// alterar valores do array
nomes[1] = "maria"
console.table(nomes)